#include "main.h"

/**
 * _abs - computes the absoulute value of integer
 * @n: the int to ckeck
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
